# Reproducibility

Keep a session directory intact. Raw bytes, parsed records, invalid reasons, options, hashes,
version, and commands provide an auditable chain. Synthetic output is stable for equal profile,
seed, and options on a compatible Datary version. Reports record assumptions and version.

