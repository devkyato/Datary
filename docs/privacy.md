# Privacy

Working-directory paths are `<redacted>` by default; enable `--include-path` deliberately.
Environment variables are never copied to sessions. Datary has no telemetry or network calls.
Raw data may itself contain secrets, so inspect permissions and redact before sharing.

